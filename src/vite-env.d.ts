/// <reference types="vite/client" />

// Replaced at build time with the version field of package.json, which is also
// the version electron-builder stamps on the packaged app. See vite.config.ts.
declare const __APP_VERSION__: string;
